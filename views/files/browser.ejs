<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>

  <body class="browser">
    <nav>NAME</nav>
    <aside>Menu</aside>
    <main>

    </main>
    <div>Browse</div>
    <a href="/browser">Home</a>
    <% breadcrumbs.forEach((folder, index, breadcrumbs)=> { %>
      >
      <a href="/browser/folder/<%= folder.id %>">
        <%= folder.name %>
      </a>
    <% }) %>
    <hr>
    <% if (view === "folder") { %>
      <%- include("./partials/folderContents") %>
    <% } else { %>
      <%- include("./partials/fileDetails") %>
    <% } %>
    </main>
    <%- include('../partials/footer') %>
  </body>

</html>