<div>Folder Contents</div>
<% if (parentId) { %>
    <a href="/browser/folder/<%= parentId %>">UP</a>
<% } else if (breadcrumbs.length> 0) { %>
    <a href="/browser">UP</a>
<% } %>
<% contents.folders.forEach((folder)=> { %>
    <a href="/browser/folder/<%= folder.id %>">
    <%= folder.name %>
    </a>
<% }) %>
<% contents.files.forEach((file)=> { %>
    <a href="/browser/file/<%= file.id %>">
    <%= file.originalname %>
    </a>
<% }) %>
<form action="/browser/folder/<%=currentFolder.id %>/upload" method="post" enctype="multipart/form-data">
    <input type="file" name="file" />
    <button type="submit">Upload</button>
</form>
<form action="/browser/folder/<%=currentFolder.id %>/createFolder" method="post">
    <input type="text" name="newFolderName" />
    <button type="submit">Create New Folder</button>
</form>